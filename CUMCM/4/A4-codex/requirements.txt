numpy==2.3.5
scipy==1.16.3
numba==0.62.1
matplotlib==3.10.6
openpyxl==3.1.5
# openpyxl is only used to read inputs and check XLSX, never to author it.
# XLSX authoring also requires Node.js and @oai/artifact-tool (Codex runtime).
