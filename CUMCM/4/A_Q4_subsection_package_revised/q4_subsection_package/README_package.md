# A题第四问 LaTeX 子节交付包（修订版）

## 文件

- `sec44_guosai.tex`：第四问主正文，从 `\subsection` 开始，可直接复制到现有论文；主正文不包含扩展相变模型内容。
- `sec44_latent_extension.tex`：单独的扩展模型内容，按需放入论文“模型改进”等位置。
- `sec44_guosai.bib`：本节使用的 3 条参考文献。
- `figures/`：图片的 PNG/PDF，文件名保持不变。

## 本次排版修改

1. 主正文公式不使用 `\boxed`。
2. 主正文不使用 `\paragraph{}`，也不使用正文加粗命令。
3. 主正文在保留模型、数值方法、表6、结果和敏感性分析的前提下进行了压缩。
4. LaTeX 中图片显示宽度进一步缩小：双栏式宽图约 `0.82\textwidth`，单图约 `0.60--0.72\textwidth`。
5. 中文图中文字仍使用 **Noto Serif CJK SC**；PDF 图仍为高分辨率栅格成图嵌入，不包含可由 Overleaf 重新解释的字体对象。

## 主结果口径

- 主方案：4 h 后保持附件1环境末值。
- 连续临界时刻：50.8243 h。
- 首个严格达标整分钟：50.8333 h = 183000 s。
- 烘干终点半径：1.2000 cm。
- 图3为几何与物性同时变化的组合比较，不作为收缩单因素结论。
- 图5为环境外推情景敏感性，不作为置信区间。
