# 03 · 问题 3 解题包(药材烘干所需时间与烘干过程水分浓度)

本目录是 **问题 3** 的完整解题包:IEEE 格式解题报告 + 独立求解代码 + 已验证结果。

- 解题报告(IEEE 格式:摘要 / 问题重述 / 假设与符号 / 数学模型 / 数值方法 / 结果 / 结论 / 参考文献):**`problem3.md`**
- 代码:`problem3.py`(问题 3 驱动,四模式 `all`/`diag`/`tables`/`verify`)、`a_model.py` + `a_output.py`(核心模型与输出层,与 A 根目录验证版一致,2026-09-11 拷贝)
- 结果:`result3.xlsx`(3431 行 × 21 列,单工作表)、`tables3.json`(表 5 原始值)、`fig3_p3.png`(表 5 剖面 + 烘干曲线)
- 校验:`compare_r3.py`(与 `out/` 已验证产物逐格比对)

## 运行(legion,conda 环境 cumcm_a)

```powershell
Set-Location D:\CUMCM\A\03
& 'D:\Applications\Anaconda3\envs\cumcm_a\python.exe' problem3.py          # 全流程: 诊断 -> 表5 -> result3.xlsx -> fig3_p3.png
& 'D:\Applications\Anaconda3\envs\cumcm_a\python.exe' problem3.py diag     # 仅量级诊断(附录3 物性 + 判据 + 自锁预估)
& 'D:\Applications\Anaconda3\envs\cumcm_a\python.exe' problem3.py tables   # 仅打印表5(读 tables3.json)
& 'D:\Applications\Anaconda3\envs\cumcm_a\python.exe' problem3.py verify   # 问题3 专属验证: 质量守恒 + 烘干时间收敛 + 与问题4 交叉复核
& 'D:\Applications\Anaconda3\envs\cumcm_a\python.exe' compare_r3.py        # 与 out/ 已验证产物逐格比对
```

附件 1 与 result3.xlsx 模板**不在本目录**(在 `D:\CUMCM\A\data`,避免官方数据混入交付物)。

**配套佐证图包**:`../03-pic/`(vivid-figures 原则生成,8 张,配色全文统一,见其 README)。

## 验证记录(2026-09-11,legion)

- **产物一致性**:`03\result3.xlsx` 与 `A\out\result3.xlsx`(已验证交付版)逐格比对,单工作表
  3431 行,**零差异**(逐格不同行 0、最大数值差 0);表 5 与 `out\tables.json` 烘干时间差 0、
  原始值最大绝对差 3.0×10⁻¹⁰(事件式与定长积分的稠密输出系数差异,4 位小数舍入后零差异)。
- **质量守恒(全流程)**:相对残差 8.186×10⁻⁷(首 1 h 按 1 s、其后 60 s 细网格通量积分;
  域积分初值 M̃(0) = 1.275000)。
- **烘干时间网格收敛**:N = 100/200/400/800 → 206263.1 / 205971.7 / 205862.4 / 205825.4 s;
  Richardson 极限 ≈ 205813.1 s(57.1703 h);N=400 误差估计 49.3 s(0.024%)。
  观测收敛阶 ≈1.4–1.6(事件时刻的误差被末端慢动力学放大 + 时间容差底噪);按 p=2 或
  p≈1.5 外推,N=400 误差均 ≤ 50 s。
- **交叉校验(与问题 4)**:中心水分曲线在 **40.98 h** 处交叉(600 s 采样插值);问题 4
  烘干时间 50.8261 h,与本文 57.1840 h 同落"2–3 天"。
- **积分成本**:全流程积分 3614 步、7.2 s(N=400);烘干时间收敛 4 个网格合计约 28 s。

## 关键结论

| 量 | 值 |
|---|---|
| 烘干时间 t* | **205862.4 s = 57.1840 h = 2.3827 天**(判据 max C < 0.15 kg/kg) |
| 网格不确定度 | ≤ 50 s(0.024%),即 57.184 ± 0.014 h |
| 首 6 h | 中心 2.55 → 1.0156(完成中心总降幅的 63.9%) |
| 末段自锁 | 末 3.18 h 中心仅降 0.0035;τ₁ 由 3.41 h 增至约 23.9 h |
| 表面 | 12 h 即降至 0.1640,结束 0.0525(烘房 C_air = 0.04986) |
| 温度 | 2 h 后全程准稳态 50.1650 °C |
| 交叉校验 | 与问题 4 在 41.0 h 交叉(问题 4: 50.8261 h) |

**不确定度(论文 V.E 节)**:干时几乎完全由 D 与"维持末值"假设决定(±5 °C → ∓14.6%/+18.2%);
蒙特卡洛 57.69±4.53 h;GAN 随机波动 std ≈0.01 h 可忽略。最不利系统偏移下干时区间
48.9–67.6 h 仍整体落在 2–3 天内。

**result3.xlsx 约定**:单工作表(与模板一致,工作表名沿用模板的 "Sheet1");列为
0, 0.1, …, 1.9 cm + 末列"2"(表面,由边界条件反解);t = 60, 120, …, 205860 s
(3431 行,60 s 间隔至烘干结束;烘干完成时刻 205862.4 s 与最后一行的 2.4 s 之差
在 60 s 输出分辨率内,表 5 的"烘干结束"行由事件时刻直接给出)。
